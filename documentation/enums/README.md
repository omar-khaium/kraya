```dart
enum Religion { muslim, hindu, buddhist, christian, others}

enum Gender { male, female, nonBinary}

enum Role { owner, tenant}

enum PropertyType { residential, commercial}

enum PropertySubType { flat, apartment, building}

enum PaymentStatus { paid, unpaid}

```
